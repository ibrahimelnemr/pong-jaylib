package org.example;

import com.raylib.Jaylib.Vector2;

public class PlayerBat extends Bat {
        public PlayerBat(Vector2 position, Vector2 size, int velocity) {
        super(position, size, velocity);
    }
}
